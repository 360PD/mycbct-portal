node_modules
.env
.env.local
.env*.local

# build output & macOS files
.next/
.DS_Store

# TypeScript incremental build cache — written by `tsc --noEmit`, not source
tsconfig.tsbuildinfo
*.tsbuildinfo

# scratch folder for files awaiting deletion — never commit
_to_delete/
